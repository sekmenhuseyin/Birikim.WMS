@model IRS_Detay
@{string tmp = ViewBag.SirketID;}
<td></td>
<td>@Model.MalKodu</td>
<td>@Model.MalKodu</td>
<td>@Html.TextBoxFor(a => a.Miktar, new { @class = "form-control input-circle Miktar" + Model.ID + "", @required = "", @onchange = "$('#Miktar" + Model.ID + "').val(this.value)" })</td>
<td>@Model.Birim</td>
<td>@Html.TextBoxFor(a => a.MakaraNo, new { @class = "form-control input-circle", @onchange = "$('#MakaraNo" + Model.ID + "').val(this.value)" })</td>
<td>
    @using (Ajax.BeginForm("UpdateList", "Purchase", new { area = "WMS" }, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "tr" + Model.ID, HttpMethod = "POST", OnSuccess = "getresult", OnFailure = "Modaldialog('Lütfen Tekrar Deneyin', 'Hata', 'Tamam', 'btn-danger');" }, new { @class = "pull-left" }))
    {
        @Html.AntiForgeryToken()
        <input type="hidden" name="ID" value="@Model.ID" />
        <input type="hidden" name="Miktar" id="Miktar@{@Model.ID}" value="@Model.Miktar" />
        <input type="hidden" name="MakaraNo" id="MakaraNo@{@Model.ID}" value="@Model.MakaraNo" />
        <button class="btn btn-primary input-circle" type="button" onclick="UpdateList(@Model.ID)">Kaydet</button>
    }
    <a class="btn btn-default input-circle" href="javascript:;" onclick="$('#submitForm').click();"> İptal </a>
</td><script>$('.Miktar@{@Model.ID}').val('@String.Format("{0:n}", Model.Miktar)');</script>