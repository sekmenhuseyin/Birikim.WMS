@model Store02
@{
    Wms12m.Business.abstractStore<Store01> StoreOperation = new Wms12m.Business.Store();
    ViewBag.Store = StoreOperation.GetList();
}
<link href="@Url.Content("~/Content/assets/global/plugins/bootstrap-touchspin/bootstrap.touchspin.css")" rel="stylesheet" type="text/css" />
<div class="row">
    <div class="col-md-12">
        <div class="portlet box green">
            <div class="portlet-title">
                <div class="caption">
                    <i class="fa fa-gift"></i>@(Model.ID > 0 ? Model.Koridor + "  Koridor Düzenle" : "Koridor Ekle")
                </div>
            </div>
            <div class="portlet-body form">
                @using (Ajax.BeginForm("CorridorOperation", "Corridor", new { }, new AjaxOptions{InsertionMode = InsertionMode.Replace,UpdateTargetId = "CorridorFrom",OnSuccess = "CorridorReturn"}, new { @class = "form-horizontal" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true)
                    <div class="form-body">
                        <div class="form-group">
                            <label class="col-md-3 control-label">Depo</label>
                            <div class="col-md-4">
                                @Html.DropDownListFor(x => x.DepoID, new SelectList(ViewBag.Store, "ID", "DepoAdi"), "Seçiniz", new Dictionary<string, object> {
                                        { "data-action-url", Url.Action("StoreList", "Corridor") },
                                        { "data-action-dropdown", "StoreId" },
                                        { "data-action-label", "Seçiniz" },
                                        { "class", "cascading form-control  input-circle" }
                                    })
                                <span class="field-validation-valid" data-valmsg-for="DepoID" data-valmsg-replace="true" style="color:red;"></span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-md-3 control-label">Koridor</label>
                            <div class="col-md-4">
                                @Html.TextBoxFor(a => a.Koridor, new { @class = "form-control input-circle", @placeholder = "Koridor" })
                                <span class="field-validation-valid" data-valmsg-for="Koridor" data-valmsg-replace="true" style="color:red;"></span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-md-3 control-label">Sıra</label>
                            <div class="col-md-4">
                                @Html.TextBoxFor(a => a.SiraNo, new { @id = "touchspin_11", @class = "form-control" })
                                <span class="field-validation-valid" data-valmsg-for="SiraNo" data-valmsg-replace="true" style="color:red;"></span>
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-md-3 control-label">Durum</label>
                            <div class="col-md-4">
                                @Html.CheckBoxFor(a => a.Aktif, new { @checked = Model.Aktif ? "checked" : string.Empty, @class = "make-switch", @data_size = "normal", @data_on_color = "success", @data_off_text = "Pasif", @data_on_text = "Aktif", @data_off_color = "danger" })
                            </div>
                        </div>
                    </div>
                    @Html.HiddenFor(a => a.ID)
                    @Html.HiddenFor(a => a.DepoID)
                    <div class="form-actions">
                        <div class="row">
                            <div class="col-md-offset-3 col-md-9">
                                <button type="submit" class="btn btn-circle green">@(Model.ID > 0 ? "Düzenle" : "Ekle")</button>
                                <button type="button" class="btn btn-circle grey-salsa btn-outline" onclick="CreateEdit('0')">Temizle</button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
<script src="@Url.Content("~/Content/assets/global/plugins/fuelux/js/spinner.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Content/assets/global/plugins/bootstrap-touchspin/bootstrap.touchspin.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Content/assets/global/scripts/app.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Content/assets/pages/scripts/components-bootstrap-touchspin.min.js")" type="text/javascript"></script>
<script type="text/javascript">
    $(".make-switch").bootstrapSwitch();
    $('.make-switch').on('switchChange.bootstrapSwitch', function (e, data) {
        $(".make-switch").val(data)
    });
    $(document).ready(function () {
        var Status = '@Model.Aktif';
        if (Status == "False") {
            $('input[type=checkbox]').prop("checked", true).trigger("change");
        }
    });
</script>
